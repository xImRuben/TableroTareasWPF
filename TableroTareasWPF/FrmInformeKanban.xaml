<Window x:Class="TableroTareasWPF.FrmInformeKanban"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Estado - Kanban" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen" Background="#F4F5F7">

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="GridInforme" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                    <ThicknessAnimation Storyboard.TargetName="GridInforme" Storyboard.TargetProperty="Margin" From="25,50,25,0" To="25,25,25,25" Duration="0:0:0.5" DecelerationRatio="0.9"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid x:Name="GridInforme" Margin="25" Background="#F4F5F7">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,30">
                <StackPanel>
                    <TextBlock Text="Informe Ejecutivo" FontSize="28" FontWeight="Bold" Foreground="#172B4D"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <TextBlock Text="📊" Margin="0,0,5,0"/>
                        <TextBlock x:Name="txtFechaInforme" Text="Generado el: 12/12/2023" Foreground="#6B778C" FontSize="14"/>
                    </StackPanel>
                </StackPanel>

                <Button HorizontalAlignment="Right" VerticalAlignment="Top" Click="BtnImprimir_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <Border Background="#EAE6FF" CornerRadius="6" Padding="12,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🖨️" Margin="0,0,8,0"/>
                            <TextBlock Text="Imprimir / PDF" FontSize="14" FontWeight="Bold" Foreground="#6554C0"/>
                        </StackPanel>
                    </Border>
                </Button>
            </Grid>

            <Grid Grid.Row="1" Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20" Margin="0,0,15,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="4"/>
                    </Border.Effect>
                    <DockPanel>
                        <Border Background="#E6F0FF" Width="50" Height="50" CornerRadius="25" DockPanel.Dock="Left" Margin="0,0,15,0">
                            <TextBlock Text="📝" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="TOTAL TAREAS" Foreground="#6B778C" FontWeight="Bold" FontSize="11"/>
                            <TextBlock x:Name="txtTotalTareas" Text="0" FontSize="32" FontWeight="Bold" Foreground="#0052CC"/>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <Border Grid.Column="1" Background="White" CornerRadius="12" Padding="20" Margin="0,0,15,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="4"/>
                    </Border.Effect>
                    <DockPanel>
                        <Border Background="#E6FFED" Width="50" Height="50" CornerRadius="25" DockPanel.Dock="Left" Margin="0,0,15,0">
                            <TextBlock Text="✅" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="COMPLETADAS" Foreground="#6B778C" FontWeight="Bold" FontSize="11"/>
                            <TextBlock x:Name="txtCompletadas" Text="0" FontSize="32" FontWeight="Bold" Foreground="#00C853"/>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="4"/>
                    </Border.Effect>
                    <DockPanel>
                        <Border Background="#FFF8E1" Width="50" Height="50" CornerRadius="25" DockPanel.Dock="Left" Margin="0,0,15,0">
                            <TextBlock Text="📈" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="% FINALIZACIÓN" Foreground="#6B778C" FontWeight="Bold" FontSize="11"/>
                            <TextBlock x:Name="txtPorcentaje" Text="0%" FontSize="32" FontWeight="Bold" Foreground="#FFAB00"/>
                        </StackPanel>
                    </DockPanel>
                </Border>
            </Grid>

            <Grid Grid.Row="2" Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20" Margin="0,0,20,0" Height="340">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="4"/>
                    </Border.Effect>
                    <ContentControl x:Name="ContenedorGrafico"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Configuración" FontWeight="Bold" FontSize="18" Margin="0,0,0,20" Foreground="#172B4D"/>

                    <TextBlock Text="Filtrar por Prioridad" Margin="0,0,0,8" Foreground="#6B778C" FontWeight="SemiBold"/>
                    <Border CornerRadius="6" BorderThickness="1" BorderBrush="#DFE1E6" Background="White" Margin="0,0,0,20">
                        <ComboBox x:Name="cmbFiltroPrioridad" SelectionChanged="CmbFiltroPrioridad_SelectionChanged" SelectedIndex="0" Padding="10" BorderThickness="0" Background="Transparent">
                            <ComboBoxItem Content="Todas"/>
                            <ComboBoxItem Content="Alta" Foreground="#FF5252" FontWeight="SemiBold"/>
                            <ComboBoxItem Content="Media" Foreground="#FFAB00" FontWeight="SemiBold"/>
                            <ComboBoxItem Content="Baja" Foreground="#00C853" FontWeight="SemiBold"/>
                        </ComboBox>
                    </Border>

                    <TextBlock Text="Tipo de Gráfico" Margin="0,0,0,8" Foreground="#6B778C" FontWeight="SemiBold"/>
                    <Border CornerRadius="6" BorderThickness="1" BorderBrush="#DFE1E6" Background="White">
                        <ComboBox x:Name="cmbTipoGrafico" SelectionChanged="CmbTipoGrafico_SelectionChanged" SelectedIndex="0" Padding="10" BorderThickness="0" Background="Transparent">
                            <ComboBoxItem Content="Circular (Pie)"/>
                            <ComboBoxItem Content="Columnas"/>
                        </ComboBox>
                    </Border>

                    <Button Content="💾 Exportar CSV" Margin="0,30,0,0" Height="45" Background="#2684FF" Foreground="White" Click="BtnExportar_Click" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>

            <DataGrid x:Name="gridDetalles" Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True" MinHeight="200"
                      HeadersVisibility="Column" Background="White" RowBackground="White" AlternatingRowBackground="#FAFBFC"
                      BorderBrush="#EBECF0" BorderThickness="1" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F4F5F7"
                      Padding="0">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Padding" Value="15,12"/>
                        <Setter Property="Background" Value="#F4F5F7"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Foreground" Value="#5E6C84"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#DFE1E6"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                        <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="TÍTULO" Binding="{Binding Titulo}" Width="*" FontWeight="SemiBold" Foreground="#172B4D"/>

                    <DataGridTemplateColumn Header="ESTADO" Width="130">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#F4F5F7" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Estado}" Foreground="#42526E" FontWeight="Bold" FontSize="11"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="PRIORIDAD" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12" Padding="10,2" HorizontalAlignment="Left" Margin="0,2">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#EBECF0"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Prioridad}" Value="Alta">
                                                    <Setter Property="Background" Value="#FFEBEE"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Prioridad}" Value="Media">
                                                    <Setter Property="Background" Value="#FFF8E1"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Prioridad}" Value="Baja">
                                                    <Setter Property="Background" Value="#E8F5E9"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Prioridad}" FontWeight="Bold" FontSize="11" HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#42526E"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Prioridad}" Value="Alta">
                                                        <Setter Property="Foreground" Value="#BF2600"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Prioridad}" Value="Media">
                                                        <Setter Property="Foreground" Value="#BF6C00"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Prioridad}" Value="Baja">
                                                        <Setter Property="Foreground" Value="#006644"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="RESPONSABLE" Binding="{Binding Responsable}" Width="150" Foreground="#5E6C84"/>
                    <DataGridTextColumn Header="FECHA" Binding="{Binding FechaCreacion, StringFormat=dd/MM/yyyy}" Width="100" Foreground="#6B778C"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </ScrollViewer>
</Window>